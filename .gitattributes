# Shell scripts must stay LF — they run on macOS/Linux (Xcode Cloud, GitHub
# Actions macOS runners) even though this repo is authored on Windows; a
# CRLF shebang line breaks real bash/sh there.
*.sh text eol=lf
